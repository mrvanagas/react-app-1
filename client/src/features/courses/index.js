const initialState = {
  courses: [],
  loading: false
}

const reducer = (state = initialState, action) => {

  // Logika, kuri tikrina action.type ir pagal tai 
  // grąžina pakitusią state kopiją
  // ...     ...     ...
  return state;
}

export default reducer;