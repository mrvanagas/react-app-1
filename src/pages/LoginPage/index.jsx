import { Box, Typography, Container } from '@material-ui/core';

const LoginPage = () => {
  return (
    <Container>
      <Box mt={4}>
        <Typography variant="h3" component="h1">Login Page</Typography>
      </Box>
    </Container>
  )
}

export default LoginPage;