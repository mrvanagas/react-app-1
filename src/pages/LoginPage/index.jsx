import { Container, Box } from '@material-ui/core';
import Form from './Form';

const LoginPage = () => {
  return (
    <Container>
      <Box my={8}>
        <Form />
      </Box>
    </Container>
  )
};

export default LoginPage;
